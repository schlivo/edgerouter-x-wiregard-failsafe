# WireGuard Failsafe Configuration
# All settings in this file are REQUIRED unless marked (optional)

# ============== WIREGUARD SETTINGS ==============
# WireGuard interface name
WG_IFACE="wg0"

# WireGuard peer IP (tunnel IP on VPS side)
WG_PEER_IP="10.11.0.1"

# WireGuard endpoint (VPS public IP)
WG_ENDPOINT="51.38.51.158"

# WireGuard port (optional, default: 51820)
# Can also be read dynamically from running interface
WG_PORT="51820"

# ============== NETWORK INTERFACES ==============
# Primary WAN interface and gateway
PRIMARY_DEV="eth0"
PRIMARY_GW="192.168.1.1"

# Backup WAN interface and gateway
BACKUP_DEV="eth1"
BACKUP_GW="192.168.2.1"

# LAN subnet for policy-based routing rules
LAN_SUBNET="192.168.10.0/24"

# ============== ROUTE METRICS (optional) ==============
# Lower metric = higher priority
METRIC_WG=40
METRIC_PRIMARY=100
METRIC_BACKUP=200

# ============== IPV6 SETTINGS (optional) ==============
# Enable IPv6 route handling
ENABLE_IPV6="false"

# IPv6 WireGuard peer address (leave empty to disable)
WG_PEER_IP6=""

# IPv6 primary gateway (leave empty to disable)
PRIMARY_GW6=""

# ============== ALERTING SETTINGS (optional) ==============
# Enable push notifications on state changes
ALERT_ENABLED="false"

# Alert URL (e.g., https://ntfy.sh/your-router-alerts)
ALERT_URL=""

# Alert method: "ntfy" or "email"
ALERT_METHOD="ntfy"
